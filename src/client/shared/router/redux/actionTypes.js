import NAME from './constants';

export const INIT = `${NAME}/Init`;
export const REDIRECT = `${NAME}/Redirect`;
export const REPLACE = `${NAME}/Replace`;
export const RETURN_TO = `${NAME}/ReturnTo`;
export const BLOCK = `${NAME}/Block`;
export const UNBLOCK = `${NAME}/Unblock`;
